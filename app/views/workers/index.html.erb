<%= render 'shared/page_title', title: 'Empleados' %>
<div class="row">
  <div class="col-md-12 text-right form-group">
    <%= link_to 'Agregar empleado <i class="fa fa-plus-circle" aria-hidden="true"></i>'.html_safe, new_worker_path, class: 'btn btn-primary' %>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <div >
      <table class="table">
        <thead>
        <tr>
          <th>Nombre</th>
          <th>Apellido</th>
          <th>No celular</th>
        </tr>
        </thead>
        <tbody>
        <% @workers.each do |worker| %>
            <tr>
              <td><%= link_to worker.name, edit_worker_path(worker) %></td>
              <td><%= worker.last_name %></td>
              <td><%= worker.phone_number %></td>
              <td><%= link_to 'Editar', edit_worker_path(worker) %></td>
              <td><%= link_to 'Eliminar', worker_path(worker), method: :delete, data: {confirm: "Dar de baja empleado"}, class: 'pull-right' %> </td>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>