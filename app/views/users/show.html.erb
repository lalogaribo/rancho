<%= render 'shared/page_title', title: "Bienvenido #{@user.name}" %>
<div class="row">
  <div class="col-md-4 col-md-offset-4 text-center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<hr>
<h2 align="left">Predios</h2>
<%= render 'predios/predio', obj: @user.predios %>
</div>