<%= render 'shared/page_title', title: "#{@user.name}'s perfil" %>
<div class="row">
  <div class="col-md-4 col-md-offset-5 center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<hr>
<h4 align="center"><%= @user.name %>Perfil</h4>
<%= render 'predios/predio', obj: @user.predios %>

</div>